
<div class='container'>

    <div class="items">


    <div class ="row" id="background">
        <div class='col-md-6'>
            <h2>Burger Me Up! </h2>
            <div id="names">
                {{#each burger}}
                    {{#unless this.devoured}}
                        <div>
                            {{this.burger_name}}
                            <form action="burger/update/{{this.id}}?_method=PUT" method="POST">
                                <input type="hidden" name="devoured" value="1">
                                <button class = "btn" type="submit">Om Nom Nom!</button>
                            </form>
                        </div>
                    {{/unless}}
                {{/each}}
            </div>
            <div class='col-md-6'>
                <h2>Add a Burger!</h2>
                <form class="create-update-form" action="/burger/create" method="POST">
                    <div class='form-group'>
                        <label for="newburger">Burger: </label>
                        <input type="text" id="newburger" name="burger">
                    </div>
                    <button class="btn" type="submit">Create It! </button>
                </form>
            </div>
            <div class ='col-md-6'>
                <ul>
                    {{#each burgers}}
                        {{#if this.devoured}}
                            <li>
                                {{this.burger_name}}
                            </li>
                        {{/if}}
                    {{/each}}
                </ul>
            </div>
            </div>
        </div>
    </div>
    </div>